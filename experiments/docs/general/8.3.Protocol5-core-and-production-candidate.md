# Protocol5 Core Results + Production Candidate (2026-03-06)

Цель: сравнить ключевые кандидаты в едином строгом протоколе (`5-fold`, group-wise by `mouse`) и выбрать рабочий baseline-кандидат.

## 1) Единый протокол (фиксируем)

- CV: `n_splits=5`
- split-policy: group-wise по `mouse`
- sample-level: `file_mean`
- основная метрика выбора: `macro_f1`
- дополнительные: `balanced_accuracy`, `accuracy`, std метрик по фолдам

Конфиги: `configs/experiment/protocol5_core/*`

## 2) Запущенные кандидаты

1. `p5_1500_file_mean_arpls_msc_catboost`
2. `p5_2900_file_mean_airpls_snv_catboost`
3. `p5_2900_file_mean_airpls_snv_logreg`
4. `p5_2900_file_mean_airpls_snv_svm`
5. `p5_2900_file_mean_airpls_emsc_catboost`

## 3) Результаты (коротко)

- `1500` best:  
  `run_id=20260306_172427_p5_1500_file_mean_arpls_msc_catboost`  
  `macro_f1=0.4099`, `balanced_acc=0.4173`
- `2900` best:  
  `run_id=20260306_172915_p5_2900_file_mean_airpls_snv_logreg`  
  `macro_f1=0.3904`, `balanced_acc=0.3931`

Сводка со стабильностью:

- `runs/protocol5_core_shortlist.csv`

## 4) Dual-window fusion в том же протоколе

Команда:

```bash
.venv/bin/python experiments/scripts/dual_window_fusion_v1.py \
  --runs-root experiments/runs \
  --run-1500 20260306_172427_p5_1500_file_mean_arpls_msc_catboost \
  --run-2900 20260306_172915_p5_2900_file_mean_airpls_snv_logreg \
  --alpha-step 0.05 \
  --output experiments/docs/artifacts/fusion_sweep_protocol5_core.csv

.venv/bin/python -m experiments.scripts.dual_window_fusion_v1 \
  --runs-root experiments/runs \
  --run-1500 20260306_172427_p5_1500_file_mean_arpls_msc_catboost \
  --run-2900 20260306_172915_p5_2900_file_mean_airpls_snv_logreg \
  --alpha-step 0.05 \
  --output experiments/docs/artifacts/fusion_sweep_protocol5_core.csv
```

Лучший fusion:

- `alpha_1500=0.85`, `alpha_2900=0.15`
- `macro_f1=0.4344`
- `balanced_acc=0.4438`
- `accuracy=0.4576`

## 5) Production candidate (текущий)

Текущий рабочий кандидат:

1. `1500` модель: `p5_1500_file_mean_arpls_msc_catboost`  
   `run_id=20260306_172427_p5_1500_file_mean_arpls_msc_catboost`
2. `2900` модель: `p5_2900_file_mean_airpls_snv_logreg`  
   `run_id=20260306_172915_p5_2900_file_mean_airpls_snv_logreg`
3. Fusion: late-fusion вероятностей с весами `0.85 / 0.15`  
   артефакт: `experiments/docs/artifacts/fusion_sweep_protocol5_core.csv`

## 6) Таблица решений (гипотеза -> изменение -> результат -> решение)

| Гипотеза | Изменение | Результат | Решение |
|---|---|---|---|
| Нужно убрать смешение протоколов | Все ключевые сравнения переведены в `5-fold` (`protocol5_core`) | Сопоставимые метрики между кандидатами | Принято |
| `1500` лидер останется сильным в строгом split | `s1_1500` лидер прогнан на `5-fold` | `macro_f1=0.4099` | Принято как базовый `1500` |
| Для `2900` catboost лучший | Прогнаны `catboost/logreg/svm` на `airpls+snv` (+ emsc catboost) | Лучший `logreg` (`macro_f1=0.3904`) | CatBoost для `2900` отклонён |
| EMSC усилит `2900` | `2900 + airpls + emsc + catboost` | `macro_f1=0.3106` (хуже SNV) | Отклонено |
| Dual-window fusion даст прирост над single-window | late-fusion sweep по `alpha` | `macro_f1=0.4344` (выше `1500` single 0.4099) | Принято |

## 7) Addendum: Step B (Preprocessing Wave) after protocol5

После реализации Step B (`despike/snip/morph/derivatives/outlier`) проведён отдельный отбор:

1. quick-screen `s2b_qs_*` (14 прогонов);
2. stability-check `s2b_sel_*` (12 прогонов, `split_seed=11/22/33`).

Короткий итог:

1. `1500`: `despike_on` не подтвердил прирост (хуже `control` по mean);
2. `2900`: `despike_on` подтвердил прирост vs `control` по mean `macro_f1`/`balanced_accuracy`.

Сводка:

1. `runs/stepb_select_stability_summary.csv`.

Практическое решение для следующей волны:

1. `1500`: оставить текущий `control` preprocess;
2. `2900`: использовать `despike_on` в следующих кандидатах и при пересборке fusion.
